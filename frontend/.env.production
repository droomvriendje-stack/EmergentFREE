# Production environment variables for the Vite build.
# VITE_API_URL must be set to the public URL of the Railway web (backend) service.
# Override this value in the Railway frontend service's environment variables.
# Example: https://your-backend-service.up.railway.app
VITE_API_URL=
